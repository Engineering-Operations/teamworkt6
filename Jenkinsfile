Pipeline {
    agent any
    stages{
        stage('1-Blessingstage'){
            steps{
                sh 'ps -es'
                sh 'sudo systemctl status jenkins'
            }
        }
    }
}
